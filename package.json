{
  "name": "serveur",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build:astro": "cd .. && astro build",
    "dev": "wrangler dev",
    "preview": "npm run build:astro && wrangler dev",
    "deploy": "npm run build:astro && wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241224.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.99.0"
  }
}
